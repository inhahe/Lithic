# Build output
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet
packages/
*.nupkg
project.lock.json

# Build artifacts
*.deps.json
*.runtimeconfig.json

# Logs / temp
stdout.txt
stderr.txt
orchestrator2.log

# Tool metadata
.orchestrator_history

# Python caches
__pycache__/
*.pyc

# Test scratch / generated data
_newfmt_catalog/
_restore_newfmt/
test_out/
test_out_new/
test_source/
test_source_orig/
test_source_reference/

# Installer build artifacts (publish staging + generated MSI/EXE bundle/PDB/wixpdb).
# The 60 MB+ self-contained installers are release deliverables, not source, so
# they stay out of history — both where the build writes them (installer\) and
# where build-msi.bat copies them (repo root).
installer/publish/
installer/*.msi
installer/*.exe
installer/*.wixpdb
/LithicBackup-*-x64.msi
/LithicBackup-*-x64.exe

# Local-only. We publish straight from this directory now instead of mirroring a
# filtered copy into D:\github (copy_to_github.bat). Also purged from history.
CLAUDE.md
todo.txt
todo2.txt
todo3.txt
go.bat
upgradelithick.bat
# Test fixtures / run output / crash logs that had been committed by accident
test_source/
test_source_orig/
test_source_reference/
test_out_new/
_restore_newfmt/
_newfmt_catalog/
crash/
catalog.db
button-style-preview.html
*.pyc
__pycache__/
# Build output -- these got committed by accident more than once
*.exe
*.dll
*.msi
*.pdb
