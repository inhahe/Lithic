<UserControl x:Class="LithicBackup.Views.RestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LithicBackup.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:RestoreViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource H2}" Text="Restore Files" />
            <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12" Margin="0,2,0,0">
                <Run Text="Backup set: " />
                <Run Text="{Binding SelectedBackupSet.Name, Mode=OneWay}"
                     FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>

        <!-- File list with checkboxes -->
        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="4" Padding="4" Margin="0,0,0,12">
            <Grid>
                <!-- Loading indicator -->
                <TextBlock Text="Loading files..."
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

                <!-- File list -->
                <ListView ItemsSource="{Binding RestorableFiles}"
                          BorderThickness="0"
                          Background="Transparent"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="30">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="File" Width="300"
                                            DisplayMemberBinding="{Binding SourcePath}" />
                            <GridViewColumn Header="Size" Width="80"
                                            DisplayMemberBinding="{Binding SizeBytes, Converter={StaticResource FileSize}}" />
                            <GridViewColumn Header="Disc" Width="100"
                                            DisplayMemberBinding="{Binding DiscLabel}" />
                            <GridViewColumn Header="Backed Up" Width="150"
                                            DisplayMemberBinding="{Binding BackedUpUtc, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <!-- Destination path -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding DestinationPath, UpdateSourceTrigger=PropertyChanged}"
                     Padding="6,4" Margin="0,0,8,0" />
            <Button Grid.Column="1" Content="Browse..."
                    Command="{Binding BrowseCommand}"
                    Padding="12,4" />
        </Grid>

        <!-- Progress area -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12"
                    Visibility="{Binding IsRestoring, Converter={StaticResource BoolToVis}}">
            <ProgressBar Minimum="0" Maximum="100"
                         Value="{Binding RestorePercentage, Mode=OneWay}"
                         Height="20" Margin="0,0,0,4"
                         Foreground="{StaticResource PrimaryBrush}" />
            <TextBlock Style="{StaticResource SubText}" Text="{Binding ProgressText}" />
            <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding CurrentFile}" />
        </StackPanel>

        <!-- Status and result -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12"
                    Visibility="{Binding IsComplete, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBlock Text="{Binding ResultDetail}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Button Style="{StaticResource PrimaryButton}"
                    Content="Restore Selected"
                    Command="{Binding RestoreCommand}"
                    Margin="0,0,8,0" />
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Padding="16,8" Margin="0,0,8,0"
                    Visibility="{Binding IsRestoring, Converter={StaticResource BoolToVis}}" />
            <Button Content="Done"
                    Command="{Binding DoneCommand}"
                    Padding="16,8" Cursor="Hand" />
        </StackPanel>
    </Grid>
</UserControl>
