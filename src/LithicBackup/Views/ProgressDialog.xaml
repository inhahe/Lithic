<Window x:Class="LithicBackup.Views.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LithicBackup.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ProgressDialogViewModel, IsDesignTimeCreatable=False}"
        Title="{Binding Title}"
        Width="440" SizeToContent="Height"
        MinWidth="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- What the operation is doing -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Message}" TextWrapping="Wrap"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" />

        <!-- Live progress detail (counts / percentage) -->
        <TextBlock Grid.Row="1"
                   Text="{Binding Detail}" TextWrapping="Wrap"
                   MinHeight="18"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,8,0,0" />

        <!-- Indeterminate activity bar -->
        <ProgressBar Grid.Row="2"
                     Height="6" IsIndeterminate="True"
                     Margin="0,14,0,0" />

        <!-- Cancel (only when the operation is cancellable) -->
        <Button Grid.Row="3"
                Content="Cancel" Click="OnCancel"
                HorizontalAlignment="Right"
                Padding="16,6" Margin="0,16,0,0" Cursor="Hand"
                IsEnabled="{Binding CanCancel}"
                Visibility="{Binding IsCancellable,
                    Converter={StaticResource BoolToVis}}" />
    </Grid>
</Window>
